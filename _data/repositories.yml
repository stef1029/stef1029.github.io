# Drives the /repositories/ page: GitHub profile cards + repo cards, fetched
# live from the github-readme-stats instance set in _config.yml.

github_users:
  - stef1029

repo_description_lines_max: 2

# Repos to feature, as owner/name. Replace with your own.
github_repos:
  - stef1029/REPO
